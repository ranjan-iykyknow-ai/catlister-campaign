# Use fake for safe local development or resend for live delivery.
EMAIL_PROVIDER=fake
RESEND_API_KEY=
RESEND_FROM_EMAIL=Catlister <campaigns@mail.example.com>
# Only set this when RESEND_FROM_EMAIL uses onboarding@resend.dev.
RESEND_DEMO_RECIPIENT=
SEND_RATE_PER_SECOND=1

# Recommended for a public Railway demo. Leave both empty for local development.
APP_PASSWORD=
SESSION_SECRET=
DATABASE_PATH=
